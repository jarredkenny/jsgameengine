class Layer {

  constructor(name, width, height, visible, tiles){
    this.name     = name;
    this.width    = width;
    this.height   = height;
    this.visible  = visible;
    this.tiles    = tiles;
  }

}

export default Layer;
